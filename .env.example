# Postgres — your TrueNAS Docker Postgres (or a local one for dev).
DATABASE_URL="postgresql://aftertaste:CHANGE_ME@db:5432/aftertaste?schema=public"

# Auth.js session-signing secret. Generate one with: openssl rand -base64 32
AUTH_SECRET="generate-a-long-random-secret"
